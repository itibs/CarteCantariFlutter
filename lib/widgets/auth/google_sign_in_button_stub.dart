import 'package:flutter/widgets.dart';

/// Non-web stub; the rendered Google button is only used on web.
Widget buildGoogleSignInButton() {
  throw UnsupportedError(
      'The rendered Google Sign-In button is only available on web.');
}
